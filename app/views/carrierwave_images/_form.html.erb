<%= form_for(@carrierwave_image) do |f| %>
  <% if @carrierwave_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carrierwave_image.errors.count, "error") %> prohibited this carrierwave_image from being saved:</h2>

      <ul>
      <% @carrierwave_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :asset %>
    <%= f.file_field :asset  %> 

    <% if f.object.asset? %>
      <br>
      <%= image_tag f.object.asset.small.url  %> <br>
        <em>The Image</em>
        <br>
      <%= f.label :remove_asset %>
    <%= f.check_box :remove_asset  %>

    <% end %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
